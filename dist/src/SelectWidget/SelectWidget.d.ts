/// <reference types="react" />
declare const SelectWidget: ({
  schema,
  id,
  options,
  label,
  required,
  disabled,
  readonly,
  value,
  multiple,
  autofocus,
  placeholder,
  rawErrors,
  onChange,
  onBlur,
  onFocus,
}: any) => JSX.Element;
export default SelectWidget;

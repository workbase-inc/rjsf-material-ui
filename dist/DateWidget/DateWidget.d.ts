/// <reference types="react" />
import { WidgetProps } from 'react-jsonschema-form';
declare const DateWidget: ({
  id,
  readonly,
  required,
  disabled,
  label,
  value,
  onChange,
  schema,
}: WidgetProps) => JSX.Element;
export default DateWidget;
